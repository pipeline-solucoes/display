import { ThemeOptions } from '@mui/material/styles';

export const displayThemeOptions: ThemeOptions = {
  pipelinesolucoes: {
    display:{
      modal:{
        background: "#fff",
        borderRadius: '0',
        boxShadow: "none",
        iconCloseColor: '#000'           
      }
    }
  },
};